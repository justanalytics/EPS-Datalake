source:
  endpoint: "lo_delta_tables"
  properties:
    query: "select\nid,\nvessel_code,\nhandling_agent,\nready_by_date,\nport,\nport_exp_ref,\ntype,\nconsolidation_point,\ncontact_point,\ndate_range,\ndelivery_date,\nurgency,\nstatus,\nremarks,\nplanned_date,\npic,\ntelephone,\nintegration_key,\n_validation_error as validation_error,\nw_source_system,\nw_src_file_name,\nw_refresh_ts,\nw_job_instance_id,\nw_business_dt,\nw_location,\nw_batch_id\nfrom \n${p_epsdl_sri_db}.rej_sri_delivery_point"
job:
  type: "ingestion"
  tags:
  - "epsdl_plp"
  description: "Sync databricks and synapse"
version: 1
schema:
  strict_schema_check: false
  schema_derivation_strategy: "source"
  discard: false
  columns: []
target:
  endpoint: "lo_dataanalytics_azuresynapseanalytics"
  properties:
    table: "${p_epsdl_plp_db}.rej_sri_delivery_point"
    format_properties:
      write_semantics: "polybase"
      max_str_length: 4000
  operation: "overwrite"
